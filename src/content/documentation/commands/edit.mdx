# edit – Modify a note

Change a note's message or its tags.

## Usage

```bash
iwashere edit <note-id> [options]
```

### Options

| Flag          | Description                         |
|---------------|-------------------------------------|
| `--message`   | New message for the note            |
| `--tags`      | Replace tags with comma‑separated list |
| `--add-tags`  | Add tags to existing ones           |
| `--remove-tags`| Remove specific tags               |

### Example

```bash
iwashere edit 4d28a474 -m "Implement JWT with refresh tokens"
# Note updated
```

## Related commands

- [show](documentation/show)
- [delete](documentation/delete)